{{ 'bloomli-trust-row.css' | asset_url | stylesheet_tag }}
<script src="{{ 'bloomli-trust-row.js' | asset_url }}" defer="defer"></script>

<section
  class="bloomli-trust bloomli-trust--pt-{{ section.settings.padding_top }} bloomli-trust--pb-{{ section.settings.padding_bottom }} color-{{ section.settings.color_scheme }} gradient"
  data-bloomli-trust-row
>
  <div class="bloomli-trust__inner">
    <div class="bloomli-trust__track" aria-label="Bloomli proof points" data-bloomli-trust-track>
      {%- for block in section.blocks -%}
        <article class="bloomli-trust__card" {{ block.shopify_attributes }}>
          {%- if block.settings.title != blank -%}
            <h3 class="bloomli-trust__title">{{ block.settings.title | escape }}</h3>
          {%- endif -%}

          {%- if block.settings.quote != blank -%}
            <p class="bloomli-trust__quote">{{ block.settings.quote | escape }}</p>
          {%- endif -%}
        </article>
      {%- endfor -%}
    </div>

    {%- if section.blocks.size > 1 -%}
      <div class="bloomli-trust__footer">
        <div class="bloomli-trust__progress" aria-hidden="true">
          <span class="bloomli-trust__progress-bar" data-bloomli-trust-progress></span>
        </div>

        <div class="bloomli-trust__controls" data-bloomli-trust-controls>
          <button class="bloomli-trust__arrow bloomli-trust__arrow--prev" type="button" aria-label="Previous proof card" data-bloomli-trust-prev>
            <span class="bloomli-trust__arrow-icon" aria-hidden="true"></span>
          </button>
          <button class="bloomli-trust__arrow bloomli-trust__arrow--next" type="button" aria-label="Next proof card" data-bloomli-trust-next>
            <span class="bloomli-trust__arrow-icon" aria-hidden="true"></span>
          </button>
        </div>
      </div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Bloomli - Trust Row",
  "tag": "div",
  "max_blocks": 6,
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-6d8ba907-aae6-4c49-9120-e1031d9cdb3f"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top",
      "min": 8,
      "max": 48,
      "step": 2,
      "default": 18
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom",
      "min": 8,
      "max": 48,
      "step": 2,
      "default": 18
    }
  ],
  "blocks": [
    {
      "type": "proof",
      "name": "Proof card",
      "settings": [
        {
          "type": "textarea",
          "id": "quote",
          "label": "Proof sentence",
          "default": "Tested for purity, label accuracy, and quality."
        },
        {
          "type": "text",
          "id": "title",
          "label": "Proof title",
          "default": "Third-party tested"
        },
        {
          "type": "select",
          "id": "icon",
          "label": "Optional icon",
          "options": [
            { "value": "none", "label": "None" },
            { "value": "flask", "label": "Flask" },
            { "value": "shield", "label": "Shield / Check" },
            { "value": "leaf", "label": "Leaf" },
            { "value": "star", "label": "Star" },
            { "value": "check", "label": "Circle check" },
            { "value": "globe", "label": "Globe / USA" },
            { "value": "refresh", "label": "Refresh / Pause" },
            { "value": "sparkle", "label": "Sparkle" }
          ],
          "default": "none"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Bloomli - Trust Row",
      "blocks": [
        {
          "type": "proof",
          "settings": {
            "quote": "Tested for purity, label accuracy, and quality.",
            "title": "Third-party tested"
          }
        },
        {
          "type": "proof",
          "settings": {
            "quote": "Daily beauty support without turning your supplement into candy.",
            "title": "2g sugar per gummy"
          }
        },
        {
          "type": "proof",
          "settings": {
            "quote": "Pectin-based gummies made without gelatin.",
            "title": "Gelatin-free"
          }
        },
        {
          "type": "proof",
          "settings": {
            "quote": "Produced with quality standards you can trust.",
            "title": "Made in the USA"
          }
        },
        {
          "type": "proof",
          "settings": {
            "quote": "Try the ritual and see how it fits.",
            "title": "30-day guarantee"
          }
        },
        {
          "type": "proof",
          "settings": {
            "quote": "Subscriptions should feel easy, not stressful.",
            "title": "Cancel anytime"
          }
        }
      ]
    }
  ]
}
{% endschema %}
